# 新类型 newtype



# 类型别名 type alias



# Sized和不定长类型DST

- 定长类型（sized）（基本类型，集合，复合类型）
- 不定长（unsized）,大小只有在程序运行时才能动态感知。

### 动态大小类型

- 切片  （注意区分切片引用和切片）
- str  
- 特征对象



总结： Rust 中常见的 `DST` 类型有: `str`、`[T]`、`dyn Trait`，**它们都无法单独被使用，必须要通过引用或者 `Box` 来间接使用** 。

